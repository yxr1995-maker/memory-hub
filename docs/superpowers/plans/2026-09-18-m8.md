# memory-hub M8 实施计划：埋点 + 主题页 + 减产 + 样本

## 概要
四个方向：E golden 扩样本（C/G 各 +7，建立 50 条新基线）、U 使用量埋点（注入页数 + MCP experience 埋点 + usage 命令）、T 聚类主题页（P3：簇 → moc/cluster-*.md 带链接与防覆盖）、R 蒸馏减产（单项目最小观察数门槛）。E 先行锁定基线，U/T/R 文件不交叉可并行。

## 全局约束
- 不动存量 wiki 已有页内容；--safe 零写入；pytest ≥ 675 基线；评测沿用配对消融口径
- 不 spawn 子代理的契约适用于所有实现者

## Task E: golden 扩样本（先执行）
- evaluation/golden.jsonl 追加 14 条（C+7、G+7，总计 50 条），样本全部取自 ~/llm-wiki 实页；必须覆盖 4 个已知未中目标的改写查询：decisions/tiktok-ad-roi-experiment-0801（C）、failures/codex-hooks-matcher-not-showing（G）、failures/tiktok-shop-inventory-warning-false-positive-2026-08-07（G，两种问法）
- 格式 {"q","expected","type"}，scope/scope_id 选填
- 验收：跑 eval 生成 50 条新基线 reports/eval-2026-09-18-golden50.md（含 hit@5/MRR/分类型）；expected 路径逐条 ls 验证存在

## Task U: 使用量埋点
- scripts/automation_core/codex_memory.py：events 表加 pages INTEGER DEFAULT 0（连接初始化时安全迁移，捕 duplicate column）；INSERT 改显式列清单；status='recalled' 时按 context 中 (?m)^来源: 计数写入 pages（不改 _recall 签名）
- mcp/server.py：recall_for_decision/read_evidence/record_episode 接 _log（src="mcp"，同 memory_search 口径写 access.jsonl）
- 新增 scripts/usage.py + memory-hub.sh usage [--days 7]：聚合 access.jsonl（REST/MCP 按 kind）+ events（按 status 计数、pages 分布）；status.sh 末尾加一行近 7 天摘要，数据缺失降级为 0 不中断
- 验收：空库/旧库迁移/聚合逻辑单测；真库 usage --days 7 输出自洽

## Task T: 聚类主题页（P3）
- scripts/automation_core/cluster.py 渲染器改产 type: moc 页：frontmatter 含 generator: memory-hub-cluster、cluster_key、member_count、scope 全套；正文 = 概述 + 成员列表（成员能解析到 wiki 页则 [[wikilink]]，否则 - [{短哈希}] ({date}) {文本} 退化）
- scripts/automation_core/orchestrator.py：aggregate 与 publish_pages_lifecycle 写出路径 notes/ → moc/；maintain 冲突处理从抛 ValueError 改为：无 generator 标记或 mtime 晚于 manifest 记录 → 判定人工编辑、跳过并记报告；自动页幂等跳过
- scripts/publish.sh type_dir 加 moc) echo "moc" 分支；存量 notes/cluster-*.md 不迁移
- 检索零改动（moc ×1.0，出链自动进 links 边表与一跳召回）
- 验收：跨日 3 obs → 生成 moc/cluster-*.md 带链接；重跑幂等；手改页不覆盖且入报告；边表含主题页出边；回归不降

## Task R: 蒸馏端减产
- scripts/distill.sh：MIN_OBS="${MEMORY_HUB_MIN_OBS:-3}"，项目观察数 < MIN_OBS 时打印原因并跳过（--llm 同规则）；全项目不达标时产 0 页 exit 0
- 既有依赖单条观察产页的测试显式设 MEMORY_HUB_MIN_OBS=1 适配
- 验收：1-2 条跳过、3 条产页的边界测试；全量回归 675 不降

## 测试与交付
每方向配新测试；全量 pytest ≥ 675；E 建 50 条基线、末尾复跑确认无回退；证据归档 .omo/evidence/

## 不做
不做看门狗自动告警；不动向量模型；不动 dashboard UI；存量 notes/cluster-* 不迁移；Obsidian 只给指引

## 假设
- 成员观察无法映射到 wiki 页时统一退化为短哈希展示
- MIN_OBS 默认 3
- 埋点沿用 access.jsonl 现有口径，不记额外内容

