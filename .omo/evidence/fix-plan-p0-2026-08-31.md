# P0 plan/spec repair evidence — 2026-08-31

Scope: documentation-only repair. Content changes are limited to `docs/superpowers/specs/2026-08-31-roadmap-full-auto-design.md` and `docs/superpowers/plans/2026-08-31-roadmap-full-auto.md`; this file is the required evidence artifact.

## P0-1 — authoritative maintain order and rollback

- Scenario: the maintain specification, global constraints, Task 10, and Task 11 all use one mutation order and checkpoint vocabulary.
- Invocation: inline `python3` contract check run from `/Users/earan/Documents/memory-hub/.worktrees/roadmap-full-auto`.
- Binary observable: exit 0; output `PASS: sequence/checkpoints/stage_exact/verify-isolation contracts agree`.
- Checked facts: the specification contains `validate -> publish pages/lifecycle -> index swap -> lint -> atomic manifest commit -> archive -> exact stage/commit`; the plan defines matching `MAINTAIN_ORDER` and `MAINTAIN_CHECKPOINTS`; manifest requires `INDEX_SWAPPED` and `LINT_PASSED`; failure tests require pages/frontmatter/index/manifest to return to the pre-operation snapshot.

## P0-2 — exact-stage baseline isolation

- Scenario: pre-existing staged files block the operation without index/stage/commit mutation; only an empty baseline can stage an after-hash-verified whitelist with exact cached equality.
- Invocation: the same inline `python3` contract check.
- Binary observable: exit 0 and the PASS line above.
- Checked facts: both documents contain `preexisting_staged`, cached-set requirements, and index-is-unchanged requirements. Task 9 contains the failing tests and pseudocode for `preexisting_staged`, verified whitelist staging, operation-only unstage, and `commit_exact()` reachability only after `exact`.

## P0-3 — isolated verify dependencies

- Scenario: specifications and Tasks 13/14 require a temporary HOME plus explicit `WIKI_PATH`, `MEMORY_HUB_DATA`, `CODEX_SESSIONS_DIR`, `CODEX_AUTOMATIONS_DIR`, `CODEX_CONFIG_FILE`, `CODEX_HOOKS_FILE`, and `AUTOMATIONS_DB`; verify is first expected to fail unseeded, then pass after fixture seeding without real-home reads.
- Invocation: the same inline `python3` contract check.
- Binary observable: exit 0 and the PASS line above.
- Checked facts: Task 13 exclusively owns `scripts/verify.sh` injection and `tests/test_verify_isolation.py`; Task 14 consumes the contract and asserts all resolved paths are fixture-root paths with `$HOME/.codex` absent.

## Structural validation

- Scenario: placeholders are absent from the two repaired documents.
- Invocation: `rg -n -i -e '\\b(TODO|TBD|FIXME|XXX)\\b' -e '<insert[^>]*>' -e '\\[fill[^]]*\\]' docs/superpowers/specs/2026-08-31-roadmap-full-auto-design.md docs/superpowers/plans/2026-08-31-roadmap-full-auto.md`.
- Binary observable: exit 1 from `rg` (no matches), interpreted by the validation harness as `PASS: no placeholder markers` and harness exit 0.

- Scenario: superseded P0 ordering/staging contracts are absent from the maintain section and plan.
- Invocation: scoped inline `python3` scan for the old manifest-before-index ordering, baseline union staging, pre-existing-stage preservation, and obsolete checkpoint combination.
- Binary observable: exit 0; output `PASS: no superseded P0 contract remains in maintain spec/plan`.

- Scenario: whitespace validity for both changed documents.
- Invocation: `git diff --check -- docs/superpowers/specs/2026-08-31-roadmap-full-auto-design.md docs/superpowers/plans/2026-08-31-roadmap-full-auto.md` and `git diff --no-index --check /dev/null docs/superpowers/plans/2026-08-31-roadmap-full-auto.md`.
- Binary observable: tracked check exit 0; untracked-plan check exit 1 solely because `--no-index` reports a diff, with no whitespace diagnostics.

- Scenario: content-path ownership.
- Invocation: `git status --short` plus `find docs/superpowers/plans -maxdepth 1 -type f -print`.
- Binary observable before recording this evidence: only modified spec and the one untracked plan were reported. This evidence file is the mandatory `.omo/evidence/` artifact and is outside product/document-content scope.

## Stop-hook rerun: captured direct outputs

The following commands were executed after the completion hook requested direct proof. No implementation command was run because this assignment owns documentation only.

```text
COMMAND: python3 P0-document-contract-audit
p0_1_spec_order=PASS
p0_1_manifest_gate=PASS
p0_1_rollback_restores_manifest=PASS
p0_1_plan_order=PASS
p0_1_plan_checkpoints=PASS
p0_1_failure_tests=PASS
p0_2_spec_preexisting_staged=PASS
p0_2_spec_index_unchanged=PASS
p0_2_spec_cached_exact=PASS
p0_2_plan_stage_gate=PASS
p0_2_plan_operation_only_unstage=PASS
p0_3_spec_explicit_variables=PASS
p0_3_spec_unseeded_failure=PASS
p0_3_task13_owns_verify=PASS
p0_3_task14_consumes_contract=PASS
p0_3_task14_all_variables=PASS
CHECK_COUNT=16 RESULT=PASS

COMMAND: ownership-audit
verify_owner=Task13
verify_isolation_test_owner=Task13
task14_consumes_without_duplicate_ownership=PASS

COMMAND: placeholder-scan
placeholder_rg_exit=1

COMMAND: git-diff-check
git_diff_check_exit=0

COMMAND: untracked-plan-whitespace-check
untracked_plan_diff_check_exit=1
untracked_plan_whitespace_diagnostics=0

COMMAND: scope-status
 M docs/superpowers/specs/2026-08-31-roadmap-full-auto-design.md
?? docs/superpowers/plans/
```

Interpretation: the `rg` placeholder scan exits 1 because it found no matches. `git diff --no-index --check` exits 1 because the new plan differs from `/dev/null`; its captured output is empty, proving zero whitespace diagnostics. The tracked `git diff --check` exits 0. The status output contains only the two owned documentation paths; `.omo/evidence/` is ignored by Git and stores this required artifact.

## Stop-hook second rerun: defect found and repaired

The second stop-hook review identified a plan-level sequencing gap: the Task 9 `stage_exact()` gate was defensive but late in the pipeline. The requested P0-2 invariant requires a non-empty baseline staged set to stop the *entire* operation before index mutation. The plan was repaired before this verification: Task 11 now checks `journal.baseline.staged` immediately after journal preparation and returns `preexisting_staged` before the stage loop. Its tests snapshot pages/frontmatter/index/manifest/cached state, require an empty stage list, and forbid add/commit calls. Task 10 now also includes an injected `publish_pages_lifecycle` failure in its rollback matrix.

```text
COMMAND: post-fix P0 document contract audit
spec_authoritative_order=PASS
spec_manifest_after_index_lint=PASS
spec_rollback_manifest_preimage=PASS
spec_preexisting_staged_before_mutation=PASS
task9_defensive_stage_gate=PASS
task11_early_pipeline_gate=PASS
task11_preindex_test=PASS
task11_preindex_snapshot=PASS
task10_publish_failure_test=PASS
task10_rollback_snapshot=PASS
verify_task_owner=PASS
verify_isolation_variables=PASS
CHECK_COUNT=12 RESULT=PASS

COMMAND: git-diff-check
git_diff_check_exit=0

COMMAND: untracked-plan-whitespace-check
untracked_plan_diff_check_exit=1
untracked_plan_whitespace_diagnostics=0

COMMAND: scope-status
 M docs/superpowers/specs/2026-08-31-roadmap-full-auto-design.md
?? docs/superpowers/plans/
```

Interpretation: the tracked whitespace check passed. The untracked plan's `--no-index` check exited 1 only because it differs from `/dev/null`; it emitted no whitespace diagnostic. The execution-level early baseline gate closes the gap between the P0-2 specification and the orchestrator plan.

## Stop-hook third rerun: independent acceptance audit

```text
COMMAND: acceptance-audit-v3
p0_1_order_spec=PASS
p0_1_manifest_gate_spec=PASS
p0_1_manifest_rollback_spec=PASS
p0_1_checkpoint_plan=PASS
p0_1_publish_failure_plan=PASS
p0_2_spec_preflight_stop=PASS
p0_2_orchestrator_preflight_stop=PASS
p0_2_no_mutation_test=PASS
p0_3_spec_isolation=PASS
p0_3_task13_sole_owner=PASS
p0_3_task14_no_duplicate_owner=PASS
spec_sha256=dd3b1a65ce5349c2976c5556b70c00a6d22a2fd3055ab5295f23603e8b2e9534
plan_sha256=12361944be547cfc28bb6b5078b3b0d416b8d727fb4dc2fd2f249f318ae8fbe2
ASSERTIONS=11 RESULT=PASS

COMMAND: line-address-audit-v3
spec_lines_215_235=READ_AND_MATCHED
plan_lines_974_1045=READ_AND_MATCHED

COMMAND: whitespace-audit-v3
tracked_diff_check=PASS

COMMAND: evidence-exists-v3
evidence_bytes_before_append=7498
evidence_nonempty=PASS
```

Judgment: all P0 clauses are present in the claimed files, including the Task 11 operation-entry baseline gate that prevents index mutation. The cited SHA-256 values bind this audit to the exact spec/plan bytes observed before this append; this append changes only the ignored evidence file.
